package com.example.delivery_service.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

public class LocationController {
//    @PostMapping("/update")
//    public ResponseEntity<String> updateLocation(@RequestBody LocationDTO locationDTO) {
//        System.out.println("Driver ID: " + locationDTO.getDriverId());
//        System.out.println("Lat: " + locationDTO.getLatitude() + ", Lon: " + locationDTO.getLongitude());
//
//        // Store in DB or update real-time cache (like Redis)
//        return ResponseEntity.ok("Location updated");
//    }
}
